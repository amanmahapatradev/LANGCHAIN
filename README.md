# LANGCHAIN

pip install uv
uv init
uv sync


Git commands ->
 git branch -m main
 git add 
 git commit -m "first commit"

For adding langchain -> uv add langchain
For removing langchain -> uv remove langchain

for activate environment ->
For Command Prompt -> .venv\Scripts\activate
For PowerShell     -> .\.venv\Scripts\Activate.ps1
For Git Bash       -> source .venv/Scripts/activate

# Python genersated files
__pycache__/
*.py[oc]
buiold/
wheels/
*.egg-info

# Vertual environments
.venv
.env

